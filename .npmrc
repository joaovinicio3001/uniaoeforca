# Alguns pacotes (ex.: @vercel/analytics) declaram peers opcionais de
# frameworks que não usamos (SvelteKit), o que faz o resolvedor do npm entrar
# em conflito com a versão de vite do vitest. Ignorar peers opcionais é seguro
# aqui e mantém o build da Vercel reprodutível.
legacy-peer-deps=true
